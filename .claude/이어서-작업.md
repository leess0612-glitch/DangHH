# 이어서 작업 — 당현함 화면 스타일 통일

> **갱신 2026-09-22 (PC 세션).** 9/21 폰(클라우드) 세션이 시작한 일을 PC 세션이 이어서 한 것까지 반영했습니다.
> **이 문서만 읽으면 앞 대화 없이도 이어서 할 수 있습니다.** 옛 판(9/21 폰 세션판)의 설명 중 틀린 것은 고쳤습니다.

---

## 한 줄 요약

danghh.com 의 **머리띠(메뉴)·발바닥(꼬리말)·아래 신청띠** 모양을 화면마다 같게 맞추는 중입니다.
9/21 에 값 어긋남을 대부분 맞췄고, **인터넷 쪽 18장은 공용 모양 파일 `css/dh-shell.css` 한 곳으로 모았습니다(1단계 9/21 · 1.5단계 9/22 라이브).**
9/22 에는 정수기 400장의 아래 띠 모양(위 선·그림자·여백)도 인터넷 쪽과 같게 맞췄습니다.

---

## 1. 지금 구조 — 모양이 어디 있나 (가장 먼저 읽을 것)

| 화면 | 머리띠·발바닥·아래 띠 모양 | 비고 |
|---|---|---|
| **인터넷 쪽 18장** (첫 화면·렌탈 2·사은품명단·약관 2·가전 + 요금표 6·셋톱 5) | **`css/dh-shell.css`** (18장 공통, **맨 먼저** 부름) + 화면마다 남은 차이 | 남은 차이는 화면 속 `<style>` 7벌과 `css/dh-plans.css` 에 있음 |
| **정수기 400장** (water/ 200 + water-c/ 200) | `css/dh-brand.css` + 아래 띠 `css/dh-bar3.css` | 아직 dh-shell 과 따로 — 「인터넷용 1 + 정수기용 1」 두 모듈 |

**모양을 고칠 때 순서**
1. 그 값이 `css/dh-shell.css` 에 있으면 → **거기 한 곳만** 고치고, 18장의 `dh-shell.css?v=` 를 올린다.
2. 없으면 → 아직 화면마다 남아 있는 값이다(화면마다 값이 달라 못 모은 308개 — 예: 사은품명단·약관 쪽 아래 띠, 머리띠 카톡 단추 일부). 해당 화면들을 고친다.
3. 정수기 쪽은 `dh-brand.css` · `dh-bar3.css`.

**JS 공용 부품** (글·동작은 이미 한 곳)

| 파일 | 맡은 일 | 주의 |
|---|---|---|
| `js/dh-footer.js` | 발바닥 글·마크업 (418장) | 글자 13px·회사명 색이 **JS 안에 박혀 있음** — CSS 로 못 바꿈(3단계 대상) |
| `js/dh-topnav.js` | 폰 메뉴 줄 (418장) | 15px/600/`#4E5968` 이 JS 안에 박혀 있음 — `#4E5968` 은 보드에 없는 색 |
| `js/dh-apply.js` | 신청창 | |
| `js/dh-inflow.js` | 유입경로 + **들어온 문 기억**(코웨이/렌탈/첫 화면) | sessionStorage `dh_door` |
| `js/dh-nocash.js` | 코웨이 금지 낱말 바꾸기 | 정수기 400장은 **늘**, 중립 15장은 **코웨이 문일 때만** 켜짐 |

**판 번호(지금)**: `dh-shell.css?v=2`(18장) · `dh-plans.css?v=19`(11) · `dh-brand.css?v=72`(400) · `dh-bar3.css?v=63`(400) ·
`dh-compare3.css?v=62`(400) · `dh-item3.css?v=61`(398) · `dh-footer.js?v=3`(418) · `dh-inflow.js?v=67`(418) · `dh-nocash.js?v=63`(415) · `dh-callhope.js?v=5`(첫 화면·렌탈 2 + dh-apply.js 안) · `dh-apply.js?v=67`(412) · `dh-brand.css?v=73`(400) · `dh-find.js?v=4`(418) · `dh-shell.css?v=3`(18).
판 번호를 빼먹었는지는 **건강검진 빠른 점검**이 잡습니다(아래 8번).
사이트가 css·js 를 **10분**(`max-age=600`)만 보관하므로, 번호를 안 올리면 재방문 손님이 최대 10분 옛 모양을 봅니다. 번호는 계속 올리세요.
⚠ **선언을 빼기만 한 파일도 내용이 바뀐 것입니다.** 1단계·1.5단계에서 `dh-plans.css` 번호를 두 번 놓쳤습니다(9/22 `efa129a2` 로 맞춤).

**값의 정본은 디자인보드**입니다 — `안티그라비티\당현함 디자인보드\디자인보드.md` (보는 판은 같은 폴더 `디자인보드.html`, 까닭은 `기록.md`).
옛 판에 적힌 "dh-brand.css 의 `:root`" 가 아닙니다. ⚠ dh-brand.css 의 `--dh-gray-900`(#191F28) 은 보드(#0F172A)와 다릅니다(미결).

---

## 2. 지금의 기준값 (2026-09-21 라이브 · 418장 실측 일치)

| 자리 | 값 |
|---|---|
| 머리띠 | 높이 **56** · 그림자 `0 2px 16px rgba(15,42,90,.08)` (요금표·셋톱의 아래 선 1px 은 없앰) |
| 메뉴 글자 | 14px · 굵기 **600** · 색 `#465368` (9/11 사장님 결정 — 굵기 500 은 쓰지 않음) |
| 메뉴 옆여백 | 12px (769~880 은 7px) · 누르는 높이 44 · 메뉴 감추는 폭 **768 이하** |
| 지금 보는 메뉴 | 연파랑 `#EFF6FF` 상자 + 파랑 `#1257C9` 700 / 마우스 올림은 연회색 `#F5F6F8` |
| 로고 | 마크~글자 틈 8 |
| 발바닥 | 바탕 `#0F172A`(보드 `--dh-c-foot`, **남색 아님**) · 글자 13/400 · 고지 12/400 · 고지 글자 흰색 **.35**(대비 3.2) |
| 신청 단추 글자 | **폰 16 · 태블릿(481~768) 18 · 컴퓨터(769~) 20** — 22 는 쓰지 않음(보드 「표 안 금액」 전용) |
| 아래 띠 모양 (인터넷·정수기 같음, 9/22~) | 위 선 **없음** · 그림자 `0 -3px 16px rgba(15,42,90,.14)`(보드 `--dh-sh-bar`) · 위아래 여백 **10** · 높이 68 (정수기 제품은 요금 줄까지 140/168) |
| 컴퓨터 떠 있는 신청 카드 | 남색 `#0B2E5C` (첫 화면·요금표 같음). **아래 띠가 아니라 별도 부품** — 흰색으로 바꾸면 흰 바탕에 묻힘 |
| 정수기 본문 자간 | `normal` (-.01em 뺌) · 기준일 안내문 `text-wrap:balance` · 규격 표 값 칸 `text-wrap:pretty` |

---

## 3. 9/21~22 에 한 일 (DangHH 커밋)

| 커밋 | 내용 |
|---|---|
| `cd7a3c5` | (폰 세션, PR #1) 1차 통일 — 메뉴 감추는 폭 768 · 로고 틈 8 · 누름높이 44 등 여덟 가지 |
| `a63ffdf2` | 2차 통일 — 메뉴 굵기 600 복구 · 요금표 머리띠 그림자 · 지금 보는 메뉴 표시 · 정수기 자간 · 고지 .35 |
| `0a9dcfd5` | **발바닥 「지원금」→「혜택」** — 9/18 발바닥을 dh-footer.js 로 모을 때부터 3일간 코웨이 전용 화면(rental-c)에 금지 낱말이 보였음 |
| `8720c078` | 요금표 떠 있는 카드 색 `#12336E` → `#0B2E5C` |
| `53dea8fe` | 코웨이 문 손님이 중립 화면(요금표·셋톱·사은품명단·약관·가전)에서 보던 「지원금·현금·입금」 차단 (dh-nocash.js) |
| `d2474fd9` | 문 기억이 `settop/x/index.html` 같은 하위 폴더 index.html 을 첫 화면으로 착각하던 것 |
| `b8ea3212` | 신청 단추 글자 폰 16 · 태블릿 18 · 컴퓨터 20 |
| `db120c74` | **공용 모양 1단계** — 인터넷 쪽 8벌에 똑같이 있던 선언 186개(규칙 49종)를 `css/dh-shell.css` 로 (2,181 → 665) |
| `9035e8da` | **공용 모양 1.5단계**(9/22) — 일부 화면에만 복사돼 있던 부품(떠 있는 카드·동그라미 단추·렌탈 입력칸 띠·지금 보는 메뉴) 선언 100개 추가 (665 → 308). 공용 파일 선언 286개 |
| `efa129a2` | 판 번호 맞춤(9/22) — `dh-plans.css` 18→19(1단계·1.5단계 때 못 올림) · `dh-inflow.js` 66→67(문 기억 고침 때 다른 세션과 겹칠까 봐 미뤘던 것). 418장, 바뀐 곳은 번호뿐 |
| `1c16da9e` | **정수기 아래 띠를 인터넷 쪽 모양으로**(9/22, 사장님 결정 「가」) — 위 선 없앰 · 그림자 남색 · 여백 12→10, `dh-bar3.css?v=63`. 띠 5 낮아짐 → **발바닥도 5 올라감**(아래 5번 참고). 파란 단추 흰 글자는 한 점도 안 바뀜 |
| `0d288791` | **가전렌탈에 구글 애널리틱스·메타 픽셀**(9/22, 사장님 결정 「통계 ㄴ」) — 415장과 같은 블록을 머리 맨 앞에. 화면 모양 변화 0, 통계 요청은 붙잡아 막고 확인 |
| `aa0b8548` | **비교함 줄이 맨 아래에서 띠를 못 따라가던 결함**(9/22) — `dh-footer.js` 목록의 옛 이름 `.cmp-bar` 를 빼고, 실제로 올린 만큼 `--dh-lift` 로 알려 `dh-compare3.css` 의 비교함 줄·말풍선이 같이 올라감(비교함 줄 높이만큼 발바닥 위 자리도 더 비움). `dh-brand.css` 의 안 쓰는 옛 비교함 규칙 180줄 지움. 판 번호 footer 3 · compare3 62 · brand 72. 맨 아래 800곳: 고치기 전 전부 덮음 → 고친 뒤 전부 붙음 |
| `eed3109d` | **렌탈 컴퓨터 입력칸 띠에 통화 희망 시간**(9/22 18:58, 사장님 「나」+「날짜 짧게」) — `#deskCallTime`(298, 모레 이후 날짜 「10/12(월)」), 골랐을 때만 memo `[통화] …`, 완료 문구 본문과 같은 규칙, 동그라미 769~980 올림. `dh-callhope.js?v=4`(목록글·안내줄로 모음, 기존 네 이름 동작 동일) · `dh-apply.js?v=66`(412장, 안의 부품 번호 v4) · index v4. 원본 보관 `당현함_보관\렌탈띠통화전_20260922\`. 라이브 확인: 신청창 412장 전수·렌탈 띠 폭 15×2·보내기 32가지(보내지 않고 내용만) 통과 |
| `ac3a41cc` | **정수기 글자색을 보드 값으로**(9/23 09:07, 사장님 「맞춰」) — `dh-brand.css` 의 `--dh-gray-900` `#191F28`→`#0F172A`(제목·본문) · `js/dh-find.js` 폰 머리띠 검색칸 글자 · `전화.html`·`카톡.html` 제목. 판 번호 `dh-brand.css?v=73`(400) · `dh-find.js?v=4`(418). 옛 사본과 420장 나란히 비교 — **색만 바뀜**(글자 덩어리 44,384곳, 자리·크기·표시 그대로) · 라이브 421장 전수 옛 색 0(카톡 노란 단추 글자 1곳은 일부러 둠). 원본 보관 `당현함_보관\정수기글자색전_20260923\`. ⚠ 같은 묶음에 다른 세션의 `dh-terms.js` 번호 통일(v1·v60 → v61)이 함께 올라감 |
| `ce69505c`·`81842938` | **「직접 적기」 완료 문구 + 카톡 글자색 한 가지로**(9/23 09:42·09:47, 사장님 「라이브에 올려」) — 완료 문구 「남기신 시간을 확인해 상담 시간 안에 …」 + 상담 시간 한 줄, 적는 칸 안내에도 상담 시간(`dh-callhope.js?v=5`, `dh-apply.js?v=67` 412장). 카톡 글자색 `#3A1D00`·`#191F28` → **`#3C1E1E`**(`dh-shell.css?v=3` 18장 · 카톡.html · 가전렌탈). 확인: 신청창 412장 전수(로컬·라이브) · 본문·신청창 흐름 · 옛 사본과 420장 비교(색 말고 0) · 라이브 노란 바탕 468곳 전부 한 값. 원본 보관 `당현함_보관\직접적기_카톡색전_20260923\` |

조사 문서(비공개): 아래 띠 전수조사(418장 × 폭 17) https://claude.ai/artifact/Q6DfLDDAWQHAaHppAVpodW

---

## 4. 남은 일 (순서대로)

| # | 할 일 | 상태 |
|---|---|---|
| 1.5 | 일부 화면에만 복사된 부품도 dh-shell 로 모으기 | **끝(9/22, 9035e8da)** — 안전 규칙에 걸린 63개는 제자리(화면마다 값이 달라서). 남은 복사본 선언 308개 |
| 3 | JS 에 박힌 모양 빼기 — dh-footer.js(13px·회사명 색) · dh-topnav.js(15px/600/#4E5968) | 작은 일 |
| 2 | 정수기(dh-brand) 와 dh-shell 합치기 | **디자인 결정 필요** — 머리띠 구조가 두 종류(인터넷 쪽만 머리띠에 전화·카톡 단추, 폰 메뉴 부품 `.mobile-menu` vs `.m-menu`, 아래 띠 `.mobile-cta-bar` vs `.bar3` — 겉모양은 9/22 같아졌지만 부품 구조는 여전히 둘) · 이름표 충돌(`--border`·`--ground`·`--dh-gray-900/950` 이 파일마다 다른 값) |

**사장님 결정 대기**
- ~~폰·태블릿 아래 띠 모양~~ → 9/22 「가」로 끝(`1c16da9e`)
- ~~컴퓨터 떠 있는 동그라미 전화·카톡이 약관 2·가전에만 없음~~ → **9/22 「다(그대로)」** — 떠 있는 단추는 긴 판매 화면용.
  가전렌탈은 짧고 화면 전체(위 단추 + 제품 칸 17개)가 이미 카톡 단추, 약관 2장은 손님이 거의 안 옴(대부분 화면에서 약관은 작은 창으로 뜸). 문서 https://claude.ai/artifact/EKYFtzv7ij3eoyusQbFhHH
- ~~세 화면에 구글 애널리틱스·메타 픽셀이 없음~~ → **9/22 「ㄴ」 가전렌탈에만 넣음**(`0d288791`, 13:55부터 방문·카톡 누름·고른 제품이 구글·메타에 잡힘 — 그 전 숫자는 네이버에만).
  약관 2장은 **일부러 없음**(네이버만). 카페 경유 화면 `전화.html`·`카톡.html` 은 **일부러 구글만**(메타 없음).
- ~~렌탈 2장 컴퓨터 입력칸 띠~~ → **9/22 「나」+「날짜 짧게」 라이브(`eed3109d`)** — 문서 https://claude.ai/artifact/XqKMeMRj2BpHBstWJmD8cK
  ⚠ 9/22 발견: 이 띠(`submitDeskForm`, 칸 deskName·deskPhone·deskService)는 **통화 희망 시간·요청사항을 안 보냄** — 9/21 통화 희망 시간 기능(dh-callhope.js 는 `#inputCallTime`·`#inputMemo` 에만 붙음)이 이 띠에만 빠짐.
  같은 화면 본문 신청서(`#apply`)에는 있음. `.sticky-form` 작은 신청서는 9/2부터 꺼져 있음(안 보임).
  시험본 3개와 9/21 통화 시험본 8개는 라이브 뒤 **휴지통으로 옮김**(사장님 「옛 시험본도 지워」, 되살릴 수 있게 휴지통).
  바꾼 것: ① 띠에 `<select id="deskCallTime">`(단추와 한 묶음 `.desk-cta-send`, 폭 298 고정 — 가장 긴 글 「10/30(일) 오후 3시 30분~4시 30분」 234 가 보이게).
     **띠 목록만 모레 이후 날짜를 「10/12(월)」로 짧게**(9/22 사장님 「날짜 짧게」 — 긴 모양이면 324 필요). 오늘·내일·완료 문구는 긴 모양 그대로
  ② 이름·번호·제품 칸 폭을 늘었다 줄었다 하게(가장 좁을 때 110·156·180, 넓으면 170·220·230) — 창 폭 약 980 아래(윈도 기준, 맥은 약 964)는 시간 칸+단추가 아랫줄(띠 142→197)
  ③ 동그라미 전화·카톡 769~980 에서 bottom 225(지금 라이브는 약 900 아래에서 띠에 겹침 — 이것도 같이 풀림)
  ④ 보낼 때 골랐으면 `memo: "[통화] 9/23(수) 오후 4시 30분~5시 30분"`, 안 골랐거나 「가능한 빨리(asap)」면 **예전과 똑같이 memo 없이**
  ⑤ 완료 문구를 본문 신청서와 같은 규칙으로(고른 시간 / 곧 / 상담 시간 아님 → 다음 영업일)
  ⑥ dh-callhope.js 에 `dh통화띠넣기·dh통화띠줄·dh통화띠완료` 추가 + 목록·완료 문구를 `목록글(처음, 직접포함, 짧게)`·`안내줄` 한 곳으로 모음
     (기존 네 이름의 동작은 3,144 시각·39만 번 호출로 옛것과 같음 · 띠 목록=본문 목록(날짜 글만 짧게) 1,524 시각 · 보내기 32번 라이브와 맞댐 — 모두 차이 0)
  건강검진 예외목록.json 「전화번호칸」 deskPhone → 통화희망시간 있음으로 고침.
- 컴퓨터에서 신청 띠가 아예 없는 9장(셋톱 5·사은품명단·약관 2·가전) — 9/22 동그라미 결정의 까닭(긴 판매 화면에만)을 따르면 약관 2·가전은 빠지고 **셋톱 5·사은품명단**만 남음(결정 필요)
- **인터넷 쪽 컴퓨터 남색 카드 → 흰 띠로 바꿀지**(7장: 첫 화면·요금표 6) — 비교 문서 https://claude.ai/artifact/3isVe7nKVupSjiLqV7FHce (가 흰 띠 추천 / 나 그대로 / 다 단추만 키우기).
  9/22 실측: 두 단추 글자는 이미 같음(20·700, 폭 769~1920 전부). 커 보인 까닭은 단추 폭 166 대 748 과 바탕(남색 위 파랑). 흰 띠로 입히면 띠 68·단추 748×48 로 정수기와 같음.
  주의: 렌탈 2장은 `#deskCtaBar` 가 입력칸 띠라 공용 파일에서 `.desk-cta-bar` 를 감추면 렌탈도 사라짐 → 7장만 따로 적용. 1600 이상은 오른쪽 상담 칸과 전화·카톡이 두 번 보임.
- ~~정수기 400장 본문 글자색~~ → **9/23 「맞춰」 라이브(`ac3a41cc`)**. 남은 것: 규격 표 항목 칸 굵기 500 · ~~카톡 노란 단추 글자색~~ → **9/23 카카오 규정대로 확정**: 글자 `rgba(0,0,0,.85)` · 기호 `#000`(보드 `--dh-c-kakao-ink`·`--dh-c-kakao-symbol`). 오전에 모았던 `#3C1E1E` 는 근거가 틀려 폐기. 라이브 전수 1,410곳 확인(421장×폭3). 사장님 「규정대로 그대로 둬」로 마무리
- ~~「직접 적기」 완료 문구~~ → **9/23 「나」안 라이브(`ce69505c`)**. 시험본 3개는 휴지통으로.

**알려진 결함 (보고만)**
- ~~비교함 줄이 화면 맨 아래에서 아래 띠를 못 따라감~~ → **9/22 고침(`aa0b8548`)**. 원인은 9/11 비교함을 새 판으로 바꾸며 옛 규칙을 안 지웠고, 9/18 발바닥 부품이 그 옛 이름을 보고 적은 것.
  ⚠ 고칠 때 한 번 더 걸린 함정: 「올린 만큼」 값을 띠가 아직 안 생긴 순간에도 알리면, 뒤늦게 생긴 비교함 줄만 화면 위쪽(20)으로 떠오름 → **실제로 띠를 올렸을 때만** 알리도록 함.
- **안 쓰는 옛 규칙이 더 남아 있음(참고)** — 건강검진 「안 쓰는 옛 규칙」 알림: dh-brand 33개 · dh-settop 21개 · dh-plans 6개 · dh-list3 1개. 글자를 이어 붙여 만드는 이름(기능 딱지 hot·cold·ice 등)은 도구가 못 알아봐 잘못 잡힐 수 있으니, 지우기 전에 화면으로 확인할 것.
- 코웨이판 만들기 도구(`안티그라비티\당현함_보관\코웨이판_만들기\coway_page.py`)에 **원래부터 안 맞는 규칙 2개**(삼행시 「당」 · 옛 발바닥 「제시된 지원금은」) — 다시 돌리면 멈춥니다.
- `js/dh-terms.js`(약관 글) 를 부르는 번호가 **두 벌** — 인터넷 쪽 17장은 `?v=1`, 정수기 400장은 `?v=60`. 9/2 이후 파일 내용이 4번 바뀌었는데 17장 쪽 번호는 한 번도 안 올라갔습니다(바뀔 때마다 10분 동안 옛 글이 섞일 수 있었음, 지금은 영향 없음). 한 번호로 맞출지 결정 대기.

---

## 5. 확인하는 방법 (9/21 PC 세션 방식)

**눈으로 보지 말고 재서 확인합니다. 표본이 아니라 전수입니다.**

1. 사본을 만든다 — `git archive HEAD | tar -x -C <사본폴더>` (라이브 폴더는 마지막까지 건드리지 않음)
2. 사본을 로컬 서버로 띄워 Playwright 로 연다. PC 는 인터넷이 되므로 **Pretendard 실제 글꼴로 잽니다**(옛 판의 "대체 글꼴" 주의는 클라우드 환경 이야기).
3. 머리띠·발바닥·아래 띠·떠 있는 카드 안의 **모든 요소의 계산값**(크기·위치·색·글자·여백·그림자 등 50여 가지)을 기록한다.
   화면 18장 × 폭 375·481·768·769·1024·1200 × 스크롤 전후 = 216화면.
4. **같은 것을 두 번 재서** 흔들린 값(글꼴 늦게 들어옴·페이지 길이)은 빼고, 고치기 전후를 대조한다 → **달라진 값 0** 이어야 통과.
5. 옮긴 선택자에 걸리는 **화면 전체 요소**도 따로 대조한다(머리띠 밖 본문에 영향이 없는지).
   1.5단계처럼 **일부 화면에만 있던 규칙**을 공용으로 옮길 때는, 그 선택자에 걸리는 요소가 있는 화면을 18장 모두 열어 찾고
   "그 화면들이 모두 원래 같은 규칙을 갖고 있었는가"를 확인한 것만 옮긴다. 마우스 올림·누름·입력칸 선택 상태도 따로 잰다.
6. 라이브에 올린 뒤에는 올라간 파일을 한 글자씩 대조하고, 실제 사이트를 같은 방식으로 다시 잰다.
7. **판 번호 점검** — 공용 css·js 마다 「파일 내용이 마지막으로 바뀐 때가 지금 번호를 올린 때보다 뒤인가」, 「한 파일을 부르는 번호가 여러 벌인가」를 git 기록으로 확인한다.
   모양을 재는 검사는 번호 누락을 못 잡습니다(서버가 `?v=` 를 무시해서 사본에서도 라이브에서도 똑같이 보임). 9/22 이 점검으로 두 파일 누락을 찾았습니다.
8. **통계 도구는 막고 잽니다** — 구글 애널리틱스·메타 픽셀·네이버 통계 요청을 막지 않으면 사본(127.0.0.1)이든 라이브든 검사할 때마다 방문으로 쏴집니다.
   구글은 사무실 필터로 걸러지지만 **메타 픽셀은 가짜 「화면 보기」로 쌓입니다**(9/21~22 검사분은 막지 않았음 — 누르기는 안 해서 성과 숫자에는 영향 없음).

**정수기 쪽을 잴 때 알아 둘 것 (9/22)**
- **발바닥은 아래 고정 띠 높이 + 12 만큼 위 여백을 둡니다**(`dh-footer.js`). 그래서 띠 높이를 바꾸면 발바닥이 따라 움직입니다.
  잴 때는 발바닥과 그 뒤 요소를 **발바닥 윗선 기준 자리**로 따로 재야 「속은 그대로, 자리만 이동」을 가릴 수 있습니다.
  이 여백은 화면을 연 뒤 잠깐 있다가 계산돼서, 열자마자 재면 기본값 `32px` 로 잡힐 수 있습니다(스크롤 뒤에 재면 늘 계산된 값).
- 발바닥이 화면에 들어오면 같은 부품이 아래 띠를 발바닥 위로 밀어 올립니다(`bottom` 값을 직접 넣음).
- 늦춰 싣기가 아닌 사진이 다 뜰 때까지 기다린 뒤 재야 화면 길이가 안 흔들립니다(기다리지 않으면 ±120 흔들림).
- 사본(`git archive`)은 **윈도우식 줄바꿈**으로 나옵니다. 작업 폴더와 한 바이트씩 대조할 때는 줄바꿈을 맞춘 뒤 비교하세요.

**꼭 볼 폭**: 375 · 481 · 768 · **769** · 880 · **881** · 1024 · 1200 (481·769·881 이 규칙이 갈리는 경계)

**코웨이 검사**는 파일 검색(grep)이 아니라 **화면 글자**(`document.body.innerText`)로 합니다. 글이 JS 로 들어가는 곳이 있어 grep 에 안 걸립니다.
코웨이 문으로 들어온 손님도 흉내 냅니다 — 먼저 `sessionStorage.setItem('dh_door','coway')` 후 열고, 신청창까지 열어서 금지 낱말 15개를 셉니다.

검사 도구(`cssparse.py`·`shell_build.py`·`shell_build15.py`·`presence.py`·`shell_snap.py`·`shell_compare.py`·`state_check.py`·판 번호 점검 `vcheck.py`·정수기 띠 `bar_snap.py`/`bar_compare.py`/`bar_apply.py` 등)는 **아직 세션 임시 폴더에만 있습니다** — 보관 폴더로 옮길지 결정 대기.

---

## 6. 하면 안 되는 것 · 주의

- **동시에 다른 세션이 DangHH 를 고칠 수 있습니다.** 9/21 에만 두 번 겹쳤습니다(경유글 `dh-inflow.js`, 통화 희망 시간 418장 판번호).
  공용 파일을 고치기 전에 `git status` 로 다른 변경이 있는지 보고, **내 파일만 골라** 커밋하세요.
- DangHH 는 **지킴이가 1분마다 자동으로 커밋·푸시**합니다(`main` = 라이브). 여러 파일을 고칠 때는 `안티그라비티\.backup_pause` 파일로 잠시 멈추되, **5분이면 풀리므로** 계속 새로 적으세요.
- 폰(클라우드) 세션에서 고치면 PC 폴더가 뒤처집니다 → 끝나면 PC 에서 `git merge --ff-only origin/main` 으로 내려받으세요. 폰 세션은 PR 로 올리세요.
- **CSS 우선순위 함정** — 같은 선택자에 앞쪽 `!important` 와 뒤쪽 일반 선언이 같이 있으면 **앞의 것이 이깁니다**.
  "뒤가 이긴다"로만 계산했다가 머리띠 전화·카톡 묶음(`.header-cta{display:none !important}`)이 되살아난 적이 있습니다(사본에서 잡음).
- **코웨이 금지 낱말 15개**(현금·지원금·지원비·상품권·페이백·캐시백·캐시·환급·사은금·축하금·보상금·장려금·리베이트·입금·계좌이체).
  공용 부품을 새로 만들거나 합칠 때는 **rental-c·water-c 를 화면 글자로 다시 검사**하세요(9/18~21 발바닥 사고).
- **`--gray` · `--w-sub` · `--w-min` 같은 눈금 자체를 바꾸지 말 것.** 여러 곳이 같이 씁니다. 머리띠·발바닥만 고칠 때는 규칙에 값을 직접 적으세요.
- **하단 신청띠의 단추 태그** — 신청창을 여는 것은 `<button type="button">`, 다른 화면으로 가는 것(privacy·terms·appliance)은 `<a href="...index.html#apply-now">`. 바꾸지 마세요.
- **`data/휴무일.json`** 은 자동 백업이 매일 건드립니다. 충돌 나면 `main` 쪽을 쓰세요.
- **정수기 견본 도구(`안티그라비티\당현함_보관\렌탈브랜드페이지_견본\만들기\`)로 화면을 다시 찍어 내지 마세요.**
  9월 11일 기준으로 멈춰 있어 라이브 정수기 400장과 한 장에 80~116줄씩 다릅니다(위 메뉴·찾기·발바닥 부품이 없고 판 번호도 낮음).
  머리띠·발바닥 통합도 라이브 파일을 직접 고치세요. `라이브옮기기.js` 에 안전장치가 있어 낡은 화면은 옮겨지지 않습니다(2026-09-21).

---

## 7. 참고 자리

| 무엇 | 어디 |
|---|---|
| **건강검진** | `안티그라비티\당현함건강검진\` (아래 8번) — 보고서 `보고서\최신.html` |
| 디자인 정본 | `안티그라비티\당현함 디자인보드\디자인보드.md` (+ `디자인보드.html` · `기록.md` · `문구규칙.md`) |
| 인터넷 쪽 공용 모양 (18장) | `css/dh-shell.css` |
| 요금표·셋톱 남은 모양 (11장) | `css/dh-plans.css` |
| 정수기 모양 (400장) | `css/dh-brand.css` · 아래 띠 `css/dh-bar3.css` · `js/dh-bar3.js` |
| 발바닥 글·마크업 (418장) | `js/dh-footer.js` |
| 폰 메뉴 줄 (418장) | `js/dh-topnav.js` |
| 코웨이 낱말 바꾸기 | `js/dh-nocash.js` · 문 기억 `js/dh-inflow.js` |
| 신청창 | `js/dh-apply.js` |
| 코웨이판 만들기 | `안티그라비티\당현함_보관\코웨이판_만들기\` |
| 앞 세션 기록 | 폰 세션 https://claude.ai/code/session_01VNEcwcW1iMaNVnoNNtvzzg · PC 세션 「당형함닷컴 페이지 스타일 통일성」 |
| 병합된 PR | https://github.com/leess0612-glitch/DangHH/pull/1 |

---

## 8. 건강검진 (2026-09-22 새로 만듦) — 고친 뒤 꼭

9/22 찾은 결함 일곱 개의 원인(같은 것 여러 벌 · 이름으로만 이어진 연결 · 안 지운 옛 코드 · 모을 때 예외 화면 놓침 · 평소 모습만 확인 · 아무 화면이나 본뜨기)을
사람이 기억하는 대신 도구가 날마다 봅니다. 위치 `안티그라비티\당현함건강검진\`.

| 언제 | 무엇을 | 어떻게 |
|---|---|---|
| **DangHH 를 고친 뒤, 올리기 전** | 빠른 점검(파일만, 약 8초) — 통계 세 가지 · 판 번호 · 끊긴 연결 · 전화번호 칸 · 안 쓰는 옛 규칙 | `python 안티그라비티\당현함건강검진\run.py quick` → **문제 0** 이어야 올림 |
| 큰 수정 뒤 · 매일 새벽 3:30 자동 | 전체 점검(라이브, 약 15분, 통계 막음) — 위 + 코웨이 금지 낱말(신청창 속까지) · 맨 아래 상태 · 화면 오류 · 통계 줄 · 모양 기준값 | `run.py full` / 작업 스케줄러 `DangHH_HealthCheck`(문제 있으면 텔레그램 🔴) |

- **일부러 다르게 둔 것**은 `예외목록.json` 에 까닭과 함께 적습니다(문제 대신 「알려진 것」으로 보임). 결정이 바뀌면 거기서 지우세요.
- 9/22 시험: 복사본에 일부러 넣은 실수 다섯(판 번호 안 올림·기록 뒤 누락·통계 코드 뺌·새 전화칸·없는 이름)을 모두 잡았고, 고치면 문제 0.
- 9/22 실제 결과: 빠른 문제 0 · 전체 문제 0(6~7분) · 알려진 것 15(남색 카드 발바닥 덮음 7곳 포함). 예약 작업 손으로 한 번 돌려 결과 코드 0 확인.
- 같은 날 다른 세션이 모든 화면에 **로봇 막음**(`navigator.webdriver` → `window.DH_BOT` · GA·메타·네이버 스스로 끔)을 넣었습니다. 건강검진은 이를 감안해 「로봇 막음이 켜졌나」로 통계 코드 동작을 확인합니다.

**고치기 전 지킬 다섯 가지**
1. 한 곳으로 모으거나 새 기능을 넣을 땐 **예외 화면 목록**부터 — 코웨이(rental-c·water-c) · 렌탈 입력칸 띠 · 약관 2 · 가전 · 경유 화면(전화·카톡.html)
2. 이름(class·id)을 바꾸면 **옛 이름은 같은 날 지운다**(끊긴 연결 점검이 남은 옛 이름을 잡음)
3. 같은 기능이 여러 벌이면(렌탈 신청서 세 벌 등) **모든 벌에** 넣었는지 — 「전화번호 칸 목록」이 보여 줌
4. 새 화면은 **첫 화면 머리(통계 세 가지 블록)** 를 본보기로
5. 확인은 **겹친 상황까지** — 맨 아래까지 내림 + 비교함 담김 + 코웨이 문 + 폰/컴퓨터
